# Server Configuration
PORT=3000
NODE_ENV=development
CORS_ORIGIN=*

# Database
MONGODB_URI="mongodb://localhost:27017/otp_service_db"

# Security
JWT_SECRET="your_super_secret_jwt_key"
JWT_EXPIRY="15m" # Verification token expiry
ACCESS_TOKEN_TTL="1h" # Access token expiry
REFRESH_TOKEN_TTL="7d" # Refresh token expiry
BCRYPT_SALT_ROUNDS=10

# SMS Provider (Optional - for development fallback)
# MSG91_API_KEY=""